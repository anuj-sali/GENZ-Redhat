{
  "name": "json-server",
  "version": "0.17.4",
  "description": "Used to make server which saves json in backend.",
  "main": "server.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "json-server -p 3001 -w db.json"
  },
  "author": "Anuj Sali",
  "license": "ISC"
}
